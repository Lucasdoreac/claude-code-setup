{
  "mcpServers": {
    "context7": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Official library documentation and code examples. Research external packages and frameworks."
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["@modelcontextprotocol/server-memory"],
      "description": "Persistent knowledge graph across sessions. Stores facts, relationships, and context between conversations."
    }
  }
}
